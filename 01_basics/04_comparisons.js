/* comparing same data type: >,<,<=,>=,==,!=,===*/
/* comparing different data types is unpredictable , should be avoided (typescript doesn't allow this)*/
/* == can compare with diff datatypes, === strictly checks for different data types.*/
